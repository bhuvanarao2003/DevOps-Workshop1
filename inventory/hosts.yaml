all:
  hosts:
    mail.example.com:
      ansible_connection: local
  children:
    webserver:
      hosts:
        canara.engineering.com:
          ansible_coonnection:local
    dbserver:
      hosts:
        bar.example.com:
          ansible_connection: local
